import {Component} from "@angular/core";

// load the template below in the component
@Component({
	templateUrl: "./templates/home.php"
})

// load the data into the following template
export class HomeComponent {}